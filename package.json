{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "aws s3 sync ./.output/public s3://alexgonz.dev",
    "gen-deploy": "npm run generate && npm run deploy"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.6.0",
    "@mdi/font": "^7.4.47",
    "@nuxt/content": "^2.13.0",
    "@nuxt/image": "^1.7.0",
    "nuxt": "^3.12.3",
    "sass": "^1.77.8",
    "vuetify": "^3.6.13-3.7.0-beta.0.0"
  }
}
